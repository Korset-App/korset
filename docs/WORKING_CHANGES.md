# Незавершённые изменения: порядок передачи

Снимок 2026-09-26. Рабочая ветка: `codex/workflow-hardening`.
Ветка создана из main без сброса или переноса существующих изменений.
Это карта незакоммиченной работы, а не утверждение готовности всех функций.

## Пакеты

| Пакет | Область | Состояние и следующая проверка |
| --- | --- | --- |
| Среда агентов | AGENTS, .codex, .agents, память, Jev, команды проверок | Настроено; локальные проверки пройдены |
| Работа без сети | src/sw.js, tests/pwa, playwright.pwa.config.js | Исправлено; проверено в браузере на сборке |
| Каталог и обогащение | scripts каталога, связанные планы | Существовавшая работа; сверять с планом catalog-v4 перед изменением |
| Каталог и карточки | экраны, компоненты, иконки, стили | Существовавшая работа; нужна визуальная проверка затронутых экранов |
| Профиль | профиль и настройки пользователя | Сверять с планом profile-screen-redesign-handoff-and-stages |
| ИИ продукта | обработчики и клиентские компоненты ИИ | Отдельная проверка поведения; не смешивать с внутренним Jev |
| Документы и прочее | остальные файлы | Проверять принадлежность к пакету перед фиксацией |

Точные пути исходных 194 записей сохранены локально:
`scratch/workflow-audit/change-inventory.json`. Это снимок, не актуальный git status.
Резервная копия изменений отслеживаемых файлов: `scratch/workflow-audit/tracked-changes.patch`.
Она не включает новые файлы; сами новые файлы сохранены в рабочем каталоге.

16 временных диагностических скриптов перемещены в
`scratch/workflow-audit/quarantine/`, без удаления содержимого.
Исходные пути и SHA-256: `scratch/workflow-audit/quarantine-manifest.json`.
Для восстановления копировать выбранный файл по исходному пути, только если путь свободен.
Архивы пользовательских запросов и используемый icon-normalization.json сохранены.

## Для следующей задачи

1. Выбрать один пакет; прочитать его план и `git status --short`.
2. При параллельной работе сообщить каждому помощнику точные разрешённые файлы.
   Ни один файл не должен одновременно иметь двух редакторов.
3. Проверить изменение узко; общий набор запускать перед передачей результата.
4. Просмотреть изменения и фиксировать только явные пути своего пакета.
   Не добавлять все файлы разом и не удалять чужую работу ради чистого состояния.
5. Передать цель, файлы, фактические проверки и оставшийся вопрос в нескольких строках.

Последняя полная проверка: 668 модульных тестов, локализация, ограничитель предупреждений,
сборка и 1 браузерный тест без сети — успешно. Это не заменяет проверку реального
магазина, разграничения доступа и товарных данных перед пилотом.
